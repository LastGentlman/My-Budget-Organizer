import styled from 'styled-components'

export const Main = styled.main `
    margin: 12px auto;
    text-align: center;
    max-width: 600px;

    & button {
        color: white;
        height: 25px;
        width: auto;
        padding: 0 0.5em;
        border: none;
        border-radius: 8px;
        background: grey;
    }
`