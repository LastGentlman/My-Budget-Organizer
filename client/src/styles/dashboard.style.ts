import styled from 'styled-components'

export const Main = styled.main `
    margin: 12px auto;
    text-align: center;
    max-width: 800px;
    height: auto;
`

export const IncomeBtn = styled.button `
        color: white;
        height: 30px;
        width: 80px;
        padding: 0 0.5em;
        border: none;
        border-radius: 8px;
        background: green;
        cursor: pointer;
`

export const ExpenseBtn = styled.button `
        color: white;
        height: 30px;
        width: 80px;
        padding: 0 0.5em;
        border: none;
        border-radius: 8px;
        background: tomato;
        cursor: pointer;
        margin-left: .5em;
`

export const LastTransactions = styled.section `

`